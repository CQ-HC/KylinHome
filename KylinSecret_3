在ubuntu上使用pip安装python的requests外部库的时候，总是出错，安装不了

解决办法：
直接使用apt-get安装：
1.请先执行apt-get -f install 修正(依赖关系)破损的软件包
2.python的requests包是有PPA源的，直接使用apt安装即可 apt-get install python-requests
3.进入python交互程序，输入import requests,如果不报错，就说明requests模块安装成功
